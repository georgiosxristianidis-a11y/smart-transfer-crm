{
  "name": "unit-calc-pro",
  "version": "1.0.14",
  "description": "Smart Calculator for Minivan Transfers",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "npx http-server . -p 12345 -c-1 -a taxi.localhost",
    "serve": "npx http-server . -p 8888 -c-1",
    "build:sw": "node scripts/build-sw.mjs",
    "docs:budget": "node scripts/check-docs-budget.mjs",
    "lhci": "npx -y @lhci/cli autorun"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-plugin-security": "^3.0.0"
  }
}
