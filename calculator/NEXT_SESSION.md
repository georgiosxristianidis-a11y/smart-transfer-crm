# NEXT_SESSION.md

## Статус
- Экосистема Senior Engineering развернута успешно.
- Код отрефакторен по номенклатуре Store/View.
- Утилиты безопасности (`esc()`, `html`) вынесены в `shared/utils.js`.
- Настроены тесты и скрипт сборки SW.

## Текущие задачи (Backlog)
- [ ] Если потребуются изменения UI/UX, убедиться что не нарушается концепция Air Minimalism и токены.
- [ ] В случае изменения налоговых ставок (НДС) или формулы резерва — обновить юнит-тесты `test/calculator.store.test.js` перед влитием логики.
- [ ] Опционально: реализовать Persistent State (сохранение значений всех слайдеров между сессиями) при необходимости, Store уже имеет заглушки под это.

## Заметки
- Любое изменение файлов требует вызова `npm run build:sw` для инвалидации кэша Service Worker'а.
- Все инъекции данных в DOM проходят через `textContent` или `html` template.
